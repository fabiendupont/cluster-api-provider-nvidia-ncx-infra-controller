---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nvidiacarbideclusters.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: NvidiaCarbideCluster
    listKind: NvidiaCarbideClusterList
    plural: nvidiacarbideclusters
    singular: nvidiacarbidecluster
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: NvidiaCarbideCluster is the Schema for the nvidiacarbideclusters
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of NvidiaCarbideCluster
            properties:
              authentication:
                description: Authentication contains credentials for accessing the
                  NVIDIA Carbide API
                properties:
                  secretRef:
                    description: |-
                      SecretRef references a Secret containing NVIDIA Carbide credentials
                      The secret must contain: endpoint, orgName, token
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                required:
                - secretRef
                type: object
              controlPlaneEndpoint:
                description: ControlPlaneEndpoint represents the endpoint used to
                  communicate with the control plane
                minProperties: 1
                properties:
                  host:
                    description: host is the hostname on which the API server is serving.
                    maxLength: 512
                    minLength: 1
                    type: string
                  port:
                    description: port is the port on which the API server is serving.
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              siteRef:
                description: SiteRef references the NVIDIA Carbide Site where the
                  cluster will be provisioned
                properties:
                  id:
                    description: ID directly specifies the Site UUID
                    type: string
                  name:
                    description: Name references a Site CRD in the same namespace
                    type: string
                type: object
              subnets:
                description: Subnets for control-plane and worker nodes
                items:
                  description: SubnetSpec defines a subnet configuration
                  properties:
                    cidr:
                      description: CIDR block for the subnet
                      type: string
                    labels:
                      additionalProperties:
                        type: string
                      description: Labels to apply to the subnet
                      type: object
                    name:
                      description: Name of the subnet
                      maxLength: 63
                      type: string
                    role:
                      description: Role of the subnet (control-plane or worker)
                      enum:
                      - control-plane
                      - worker
                      type: string
                  required:
                  - cidr
                  - name
                  type: object
                minItems: 1
                type: array
              tenantID:
                description: TenantID is the NVIDIA Carbide tenant ID for multi-tenancy
                type: string
              vpc:
                description: VPC configuration for the cluster network
                properties:
                  description:
                    description: Description for the VPC
                    type: string
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels to apply to the VPC
                    type: object
                  name:
                    description: Name of the VPC
                    maxLength: 63
                    type: string
                  networkSecurityGroup:
                    description: NetworkSecurityGroup configuration
                    properties:
                      name:
                        description: Name of the Network Security Group
                        maxLength: 63
                        type: string
                      rules:
                        description: Rules for the Network Security Group
                        items:
                          description: NSGRule defines a single security rule
                          properties:
                            action:
                              description: Action to take (allow or deny)
                              enum:
                              - allow
                              - deny
                              type: string
                            direction:
                              description: Direction of traffic (ingress or egress)
                              enum:
                              - ingress
                              - egress
                              type: string
                            name:
                              description: Name of the rule
                              maxLength: 63
                              pattern: ^[a-z0-9]([a-z0-9-]*[a-z0-9])?$
                              type: string
                            portRange:
                              description: PortRange specifies the port range (e.g.,
                                "80", "1000-2000")
                              pattern: ^[0-9]{1,5}(-[0-9]{1,5})?$
                              type: string
                            protocol:
                              description: Protocol (tcp, udp, icmp, or all)
                              enum:
                              - tcp
                              - udp
                              - icmp
                              - all
                              type: string
                            sourceCIDR:
                              description: SourceCIDR specifies the source IP range
                              type: string
                          required:
                          - action
                          - direction
                          - name
                          - protocol
                          type: object
                        type: array
                    required:
                    - name
                    type: object
                  networkVirtualizationType:
                    description: |-
                      NetworkVirtualizationType specifies the network virtualization type
                      Valid values: ETHERNET_VIRTUALIZER, FNN
                    enum:
                    - ETHERNET_VIRTUALIZER
                    - FNN
                    type: string
                  nvLinkLogicalPartitionId:
                    description: NVLinkLogicalPartitionID attaches an NVLink logical
                      partition at the VPC level
                    type: string
                  vni:
                    description: Vni specifies an explicit VNI for the VPC
                    format: int32
                    type: integer
                required:
                - name
                - networkVirtualizationType
                type: object
              vpcPrefixes:
                description: VPCPrefixes for physical interface allocations (alternative
                  to Subnets for FNN VPCs)
                items:
                  description: VPCPrefixSpec defines a VPC Prefix configuration (physical
                    interface alternative to subnets)
                  properties:
                    cidr:
                      description: CIDR block for the VPC Prefix
                      type: string
                    name:
                      description: Name of the VPC Prefix
                      maxLength: 63
                      type: string
                    role:
                      description: Role of the VPC Prefix (control-plane or worker)
                      enum:
                      - control-plane
                      - worker
                      type: string
                  required:
                  - cidr
                  - name
                  type: object
                type: array
            required:
            - authentication
            - siteRef
            - subnets
            - tenantID
            - vpc
            type: object
          status:
            description: status defines the observed state of NvidiaCarbideCluster
            properties:
              conditions:
                description: Conditions represent the current state of the NvidiaCarbideCluster
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              failureMessage:
                description: |-
                  FailureMessage will be set in the event that there is a terminal problem
                  reconciling the cluster and will contain a more descriptive value suitable
                  for human consumption.
                type: string
              failureReason:
                description: |-
                  FailureReason will be set in the event that there is a terminal problem
                  reconciling the cluster and will contain a succinct value suitable for
                  machine interpretation.
                type: string
              networkStatus:
                description: NetworkStatus contains the network infrastructure status
                properties:
                  allocationID:
                    description: AllocationID is the NVIDIA Carbide Allocation ID
                    type: string
                  childIPBlockID:
                    description: ChildIPBlockID is the tenant-owned child IP block
                      derived from the allocation
                    type: string
                  ipBlockID:
                    description: IPBlockID is the NVIDIA Carbide IP Block ID used
                      for subnet allocation
                    type: string
                  nsgID:
                    description: NSGID is the Network Security Group ID
                    type: string
                  subnetIDs:
                    additionalProperties:
                      type: string
                    description: SubnetIDs maps subnet names to their IDs
                    type: object
                  vpcPrefixIDs:
                    additionalProperties:
                      type: string
                    description: VPCPrefixIDs maps VPC Prefix names to their IDs
                    type: object
                type: object
              ready:
                description: Ready indicates if the cluster infrastructure is ready
                type: boolean
              vpcID:
                description: VPCID is the NVIDIA Carbide VPC ID
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
